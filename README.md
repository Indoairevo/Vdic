# Vdic
Full-stack school ERP built with Vite (React) and an Express API.

## Quick Start
- Install dependencies: `npm install`
- Start the full-stack dev server: `npm run dev`
- Open http://localhost:3000

See `RUNNING.md` for detailed setup, seeding, and Docker/Postgres notes.
